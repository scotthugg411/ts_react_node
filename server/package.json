{
  "name": "server",
  "version": "1.0.0",
  "description": "server for test project",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "build:watch": "tsc -w",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Scott Huggins & Mike Cass",
  "license": "",
  "dependencies": {
    "express": "^4.17.2"
  },
  "devDependencies": {
    "@types/node": "^17.0.10"
  }
}
